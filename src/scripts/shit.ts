#!/usr/bin/env lolcat
// this file provides "user-friendly" error message strings if needed for the memes
// if you're seeing this comment something either went really wrong in the program (sorry) or you (sorry), you're contributing (thank you), you're enjoying the rainbow (this too shall pass), or all of the above.

export default function shit(reason = "... well, we don't know...") {
    return `shit, something bad happened (probably because ${reason})
so it'd be *really* nice if *someone* were to report this...
 - kyle`;
}
